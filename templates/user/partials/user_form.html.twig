{{ form_row(form.username) }}
{{ form_row(form.password) }}
{{ form_row(form.email) }}
{{ form_row(form.admin) }}

<div class="form-actions">
    <button type="submit" class="btn btn-primary d-flex">
        <i class="fa {{ buttonIcon }} mr-4 align-self-center"></i>{{ action }}</button>
</div>

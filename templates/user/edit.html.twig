{% extends 'base.html.twig' %}

{% block header_img %}{% endblock %}

{% block body %}
    <h1>Modifier <strong>{{ user.username }}</strong></h1>
    <div class="mt-md-4">
        {{ form_start(form, {'action' : path('edit_user', {'userId' : user.id}) , 'attr': {'class': 'd-flex flex-column custom-form'}}) }}

        {% include 'user/partials/user_form.html.twig' %}

        <div class="form-actions align-self-end justify-content-between">
            <button type="submit" class="btn btn-primary">Modifier</button>
        </div>
        {{ form_end(form) }}
    </div>

    <a href="{{ path('list_users') }}" class="btn btn-primary-open mt-4">Retour à la liste</a>
{% endblock %}

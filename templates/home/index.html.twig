{% extends 'base.html.twig' %}

{% block body %}
    <div class="jumbotron jumbotron-fluid">
        <div class="container">
            <h1 class="display-2">Fluid jumbotron</h1>
            <img class="slide-image" src="{{ asset('img/todolist_homepage.jpg') }}" alt="todo list" height="150">
            <p class="lead">This is a modified jumbotron that occupies the entire horizontal space of its parent.</p>
        </div>
    </div>


    <div class="row">
        <a href="{{ path('create_task') }}" class="btn btn-success">Créer une nouvelle tâche</a>
        <a href="{{ path('list_tasks') }}" class="btn btn-info">Consulter la liste des tâches à faire</a>
        <a href="" class="btn btn-secondary">Consulter la liste des tâches terminées</a>
    </div>
{% endblock %}
